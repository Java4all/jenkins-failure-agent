# Core dependencies
requests>=2.31.0
pyyaml>=6.0.1
python-dateutil>=2.8.2
python-dotenv>=1.0.0

# Jenkins API
python-jenkins>=1.8.0

# Git integration
gitpython>=3.1.40

# HTML parsing (for doc import)
beautifulsoup4>=4.12.0

# AI client (OpenAI-compatible)
openai>=1.12.0

# AWS Bedrock support (optional - install if using Bedrock)
# pip install boto3
boto3>=1.34.0

# CLI and utilities
click>=8.1.7
rich>=13.7.0
tabulate>=0.9.0

# Web server (for service mode)
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
# Required for FastAPI File()/Form() (e.g. POST /training/restore multipart uploads)
python-multipart>=0.0.9

# Async support
aiohttp>=3.9.0
aiofiles>=23.2.0

# Notifications (optional)
slack-sdk>=3.26.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
